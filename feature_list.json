{
  "phase": "Phase 3 - reproducibility and export",
  "exit_criterion": "PROPOSAL.md section 16: \"Two models differing only in preprocessing can be compared step by step; an exported model reproduces application predictions within tolerance in a clean environment.\" Concretely, and demonstrated rather than reasoned: the application exports a fitted PLS model as the three artifacts section 9 names, a script installs nothing but NumPy in a fresh environment, runs the exported snippet against the same spectra, and the predictions agree with the served ones within a tolerance this list states; and the comparison view shows, for two experiments on one dataset, exactly which pipeline nodes differ and what each model scored. The phase ends when both are recorded with their evidence.",
  "status_values": [
    "not_started",
    "in_progress",
    "blocked",
    "passing"
  ],
  "features": [
    {
      "id": "experiment-history",
      "priority": 1,
      "area": "backend",
      "issue": 209,
      "title": "An experiment history, not only the last run",
      "depends_on": [],
      "user_visible_behavior": "The outline lists every experiment this project has run, newest first, with its status, its pipeline hash and its headline metric; opening one shows what it ran and what it scored.",
      "verification": [
        "uv run pytest - read_experiments returns every recorded run in order and GET /experiments serves them; the existing /experiments/current is unchanged",
        "pnpm typecheck && pnpm lint && pnpm test && pnpm build",
        "pnpm exec playwright test - the seeded project lists more than one experiment after a second run"
      ],
      "status": "passing",
      "evidence": "2026-09-18. Backend: project.read_experiments(directory, limit) returns every recorded run newest first, in read_experiment's own order so the two cannot disagree about which run is current, and skips a row that no longer validates rather than refusing the history; GET /api/experiments serves a summary row per run - id, status, timestamps, pipeline hash, node count, dataset version, error and the headline metrics - and GET /api/experiments/{id} now opens any run in the history, with `current` still meaning the newest. tests/test_experiment_history.py (3 tests): three runs come back newest first with read_experiment as the head and limit honoured, an empty project has an empty history, and a row whose document is not an Experiment costs only itself - written later than the good one, so a reader that gave up on the first bad row would return nothing. test_the_history_lists_every_run_newest_first_and_each_one_opens in test_server.py runs two different recipes and asserts the order, the differing pipeline hashes, the exact row keys and that every row opens to a record whose snapshot has the node count the row claimed. `uv run pytest` 868 passed 1 skipped; ruff, format, mypy clean. Frontend: useExperiments and useExperimentRecord; the outline lists every run with runLabel numbering from the project's first so a number does not move and runFigure carrying whichever headline the run has, or its status when it did not succeed; ExperimentView renders the record in four sections - what it ran (the snapshot as a node table), against what, what it scored, where - with a failed run's cause in an alert. experiments.test.ts (3 tests) covers the two helpers. `pnpm test` 126 passed; typecheck, lint, build clean. e2e `the outline lists every run, and one opens to what it ran` (runs project, which by then has several runs): the drawn count equals the served history, the newest is first, and opening it shows the record rather than the placeholder. Against HEAD's screens and backend it FAILED at `expect(received).toBeGreaterThan(expected)` - the outline drew one row however many runs there were. With the fix `pnpm exec playwright test` 58 passed (1.6m).",
      "notes": "Pagination deliberately not added: a project's runs are tens, and api.py's note on deferred pagination applies - fixing the shape of an answer before there is a question. The row is a summary because a list of thirty runs each carrying a ten-fold split's index sets is megabytes to draw thirty names; the whole record stays behind experiments/{id}. lineage-comparison is the next thing to hang from this."
    },
    {
      "id": "model-artifact",
      "priority": 1,
      "area": "backend",
      "issue": null,
      "title": "The model artifact: one self-describing file",
      "depends_on": [
        "experiment-history"
      ],
      "user_visible_behavior": "A fitted estimator can be saved as one file that carries the pipeline definition, the fitted parameters, the provenance record and the metrics, and that file can be read back without this application.",
      "verification": [
        "A documented schema under docs/, written before the writer",
        "uv run pytest - an artifact round-trips through the writer and reader; a file written by a newer schema version is refused by name",
        "The artifact opens with nothing but the standard library and NumPy, asserted in the test"
      ],
      "status": "not_started",
      "evidence": "",
      "notes": "PROPOSAL.md section 8.4. `models.py` already has an unused `Model` with `artifact_path` and `artifact_hash`; the file format is what is missing. Openly documented and readable without the application is the requirement, so a zip of JSON plus .npy rather than a pickle."
    },
    {
      "id": "model-registry",
      "priority": 2,
      "area": "frontend",
      "issue": null,
      "title": "A model registry: which models this project holds",
      "depends_on": [
        "model-artifact"
      ],
      "user_visible_behavior": "Saving a fitted estimator records it in the project with its name, its task, the experiment it came from and its metrics; the outline lists them and one can be opened.",
      "verification": [
        "uv run pytest - a saved model is recorded and served; its artifact reference is a path and never contents",
        "pnpm exec playwright test - a model saved from the analysis tab appears in the outline"
      ],
      "status": "not_started",
      "evidence": "",
      "notes": "The `model` table and the `Model` schema exist. The database stores the reference; the artifact is a file, per PROPOSAL.md section 11."
    },
    {
      "id": "json-and-snippet-export",
      "priority": 1,
      "area": "backend",
      "issue": null,
      "title": "JSON model and a standalone Python prediction snippet",
      "depends_on": [
        "model-artifact"
      ],
      "user_visible_behavior": "A fitted regression exports as a documented JSON model - the preprocessing steps with their parameters, the coefficients, the intercept and the scaling - and as a self-contained Python function that depends only on NumPy and reproduces the application's predictions.",
      "verification": [
        "A documented JSON schema under docs/, written before the writer",
        "uv run pytest - the exported snippet, executed in a subprocess with only NumPy importable, reproduces the served predictions within the parity harness's prediction tolerance",
        "The same test covers a chain that cannot be folded: the snippet carries the residual preprocessing and still reproduces the predictions"
      ],
      "status": "not_started",
      "evidence": "",
      "notes": "PROPOSAL.md section 9. `regression.coefficients_original_units` already folds a foldable chain and says by name when it cannot (#144), and `/results/{id}/coefficients` serves it. The constraint section 9 places on the codebase - exported predictions match in-application predictions within a stated tolerance, verified in CI - is this entry's verification, not a promise."
    },
    {
      "id": "lineage-comparison",
      "priority": 1,
      "area": "frontend",
      "issue": null,
      "title": "Two experiments compared step by step",
      "depends_on": [
        "experiment-history"
      ],
      "user_visible_behavior": "Two experiments on one dataset are shown side by side with exactly which pipeline nodes differ - added, removed or edited - and what each scored, so a reader can see that two models differ only in their scatter correction.",
      "verification": [
        "pnpm test - the diff of two pipeline snapshots is pure and tested: a node added, a node removed, a parameter changed, and a graph that differs only in layout reported as identical",
        "pnpm exec playwright test - the seeded project compares two experiments and names the node that differs"
      ],
      "status": "not_started",
      "evidence": "",
      "notes": "PROPOSAL.md section 8.3 calls this the single feature most likely to make a researcher prefer this tool to a notebook, and says it is free once the model is correct. `Experiment.pipeline_snapshot` is the snapshot by value that makes it possible; `CompareResults` already compares two results and is the screen to build on."
    },
    {
      "id": "html-report",
      "priority": 3,
      "area": "backend",
      "issue": null,
      "title": "An HTML report of one experiment",
      "depends_on": [
        "experiment-history",
        "lineage-comparison"
      ],
      "user_visible_behavior": "One experiment exports as a standalone HTML file carrying its pipeline, its metrics, its plots and its provenance, openable in a browser with no application and no network.",
      "verification": [
        "uv run pytest - the report is one file, references nothing external, and carries the experiment's own numbers",
        "pnpm exec playwright test - the exported file opens and shows the scores plot"
      ],
      "status": "not_started",
      "evidence": "",
      "notes": "PROPOSAL.md section 16 names HTML/PDF reporting. HTML first and PDF deferred: a browser prints a PDF and a PDF library is a dependency with weight, which section 7's rule is about."
    },
    {
      "id": "phase-3-exit-run",
      "priority": 0,
      "area": "process",
      "issue": null,
      "title": "The exit criterion demonstrated: an exported model predicts in a clean environment",
      "depends_on": [
        "json-and-snippet-export",
        "lineage-comparison"
      ],
      "user_visible_behavior": "Recorded, not built: a fitted model is exported from the served application, its snippet is run in a fresh environment with only NumPy installed, and its predictions agree with the application's within the stated tolerance; and the comparison view is shown naming the one node two models differ by.",
      "verification": [
        "A script under tests/ that drives the served application over HTTP, exports the model, runs the snippet in a subprocess with an isolated environment, and compares",
        "Its output committed under docs/phase-3/ with the date, the dataset, the pipeline, every number compared and the tolerance each is judged against"
      ],
      "status": "not_started",
      "evidence": "",
      "notes": "The shape Phase 2's exit run set: tests/exit_run.py drives the application over HTTP and writes docs/phase-2/exit-run.md, and this is the same thing for section 16's Phase 3 criterion. The float32 store finding from that run applies here too - an exported snippet computing in float64 will differ from the served numbers at the level docs/phase-2/exit-run.md records, and the tolerance has to be stated with that in mind."
    }
  ]
}
