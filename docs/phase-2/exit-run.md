# Phase 2 exit run

Generated by `uv run python -m tests.exit_run` on 2026-09-18 at 07:54 UTC. **Do not edit by hand**: rerun the script.

`PROPOSAL.md` §16, Phase 2: *a complete calibration workflow on a real NIR dataset matches reference software within stated tolerance.* `feature_list.json` decides what that means - the workflow driven over HTTP against an independent PLS on the experiment's own resolved folds, within the parity tolerances - and this is that run.

## Verdict: **Met** on every quantity the criterion names - RMSECV, Q², the calibration metrics, the curve and both prediction sets - at the parity tolerances, once the reference is narrowed to float32 where the store narrows; not against a float64 reference, and the caveat below says why. The coefficient vector, compared as well, is outside the coefficient class's tolerance under both; the section at the end says on which side the difference lies.

## What was run

- **Application:** chemometrics-workbench 0.6.0, Python 3.12.3, numpy 2.5.2, scipy 1.18.1, on Linux-6.8.0-139-generic-x86_64-with-glibc2.39.
- **Reference:** scikit-learn 1.9.0 `PLSRegression(scale=False)`, scipy 1.18.1 `savgol_filter(mode="interp")`, SNV in NumPy 2.5.2 (ddof 1). Nothing from `chemometrics_workbench` on this side.
- **Dataset:** Tecator, 240 × 100, imported through `/import` from the CSV `tests/seed_e2e.py` writes; content hash `sha256:858b4e6c30bdb8416a4b5843b9891e1932ff79d296337ed16fd28db29f3c19db`, reader `delimited 1`. The reference parses the same CSV.
- **Pipeline:** source → SNV → Savitzky-Golay (window 11, order 2, derivative 1) → k-fold 10 (shuffle, seed 42) → mean centre → PLS 5 LV on `fat`. Validation answered `valid: True` with 0 problems.
- **Folds:** the 10 test sets the experiment recorded, sizes [24, 24, 24, 24, 24, 24, 24, 24, 24, 24], handed to the reference as index arrays. Never reseeded on the reference side.
- **Run:** job `0af3da2d-9866-46a1-9ea2-e949724af04a` ended `succeeded`; experiment `f14312f8-f842-42b6-803f-891d47a1089f`.
- **Folded coefficients:** `/results/pls/coefficients` answered `available: False` - SNVTransformer cannot be folded into a coefficient vector: it depends on the sample being predicted, so it is not a fixed linear map on X and has to be re-executed at prediction time (pls-regression.md §7). Export it as part of a residual preprocessing chain instead.

## Comparison against a float64 reference

The reference runs in float64 throughout. The served numbers come from arrays the executor stored as float32 between nodes (`PROPOSAL.md` §13).

| Quantity | Served | Reference | max abs diff | max rel diff | Class (rtol, atol) | Result |
| --- | --- | --- | --- | --- | --- | --- |
| RMSECV | 2.390922629 | 2.390921117 | 1.51e-06 | 6.32e-07 | metrics (1e-06, 1e-09) | pass |
| Q² | 0.9721718715 | 0.9721719067 | 3.52e-08 | 3.62e-08 | metrics (1e-06, 1e-09) | pass |
| RMSEC (fold 0) | 2.243223799 | 2.243222134 | 1.67e-06 | 7.42e-07 | metrics (1e-06, 1e-09) | pass |
| R² (fold 0) | 0.9754737692 | 0.9754738056 | 3.64e-08 | 3.73e-08 | metrics (1e-06, 1e-09) | pass |
| RMSEP (fold 0 held out) | 2.212189391 | 2.212189869 | 4.79e-07 | 2.16e-07 | metrics (1e-06, 1e-09) | pass |
| RMSECV curve, A = 1…5 | 5 values | 5 values | 1.51e-06 | 6.32e-07 | metrics (1e-06, 1e-09) | pass |
| coefficients b (node axis, fold 0) | 100 values | 100 values | 0.00058 | 0.00548 | coefficients (1e-06, 1e-09) | FAIL |
| calibration predictions (fold 0) | 216 values | 216 values | 3.5e-05 | 7.09e-06 | predictions (1e-06, 1e-09) | FAIL |
| held-out predictions (fold 0) | 24 values | 24 values | 1.61e-05 | 2.19e-06 | predictions (1e-06, 1e-09) | FAIL |

## Comparison against the reference narrowed to float32 where the store narrows

The same reference, with the imported matrix and each intermediate array rounded to float32 - at import, after SNV, after Savitzky-Golay and after the per-fold centring, the four points at which the executor reads an array back from the store.

| Quantity | Served | Reference | max abs diff | max rel diff | Class (rtol, atol) | Result |
| --- | --- | --- | --- | --- | --- | --- |
| RMSECV | 2.390922629 | 2.390922623 | 6.09e-09 | 2.55e-09 | metrics (1e-06, 1e-09) | pass |
| Q² | 0.9721718715 | 0.9721718716 | 1.42e-10 | 1.46e-10 | metrics (1e-06, 1e-09) | pass |
| RMSEC (fold 0) | 2.243223799 | 2.243223805 | 5.7e-09 | 2.54e-09 | metrics (1e-06, 1e-09) | pass |
| R² (fold 0) | 0.9754737692 | 0.9754737691 | 1.25e-10 | 1.28e-10 | metrics (1e-06, 1e-09) | pass |
| RMSEP (fold 0 held out) | 2.212189391 | 2.212189392 | 1.55e-09 | 7e-10 | metrics (1e-06, 1e-09) | pass |
| RMSECV curve, A = 1…5 | 5 values | 5 values | 6.09e-09 | 2.55e-09 | metrics (1e-06, 1e-09) | pass |
| coefficients b (node axis, fold 0) | 100 values | 100 values | 6.63e-06 | 2.87e-06 | coefficients (1e-06, 1e-09) | FAIL |
| calibration predictions (fold 0) | 216 values | 216 values | 6.39e-07 | 3.68e-08 | predictions (1e-06, 1e-09) | pass |
| held-out predictions (fold 0) | 24 values | 24 values | 6.56e-08 | 6.9e-09 | predictions (1e-06, 1e-09) | pass |

## Reading the two tables

- The parity tolerances are `tests/parity.py`'s, by class, and were not adjusted for this run.
- A first-derivative chain amplifies the float32 rounding of the stored arrays: the derivative of a rounded spectrum differs from the derivative of the exact one by the rounding scaled by the filter's weights. Where the float64 comparison fails and the narrowed one passes, that rounding is the whole difference, and it is a property of the store the specification chose, not of the arithmetic.
- `RMSEC`, `R²`, `RMSEP`, the coefficients and both prediction sets are fold zero's model, which is the one the executor fits and serves (`executor.py`, *Estimators*). `RMSECV`, `Q²` and the curve pool every fold.

## Where the coefficient difference sits

The coefficient vector is the one quantity outside its parity tolerance under both comparisons. Both sides approximate the same `b = W(P'W)⁻¹q`; an 80-bit NIPALS written for this script alone, on the narrowed fold-zero matrix, says which side is nearer it. Distances are the largest element-wise difference over the largest coefficient.

| Coefficient vector | Distance from the extended-precision vector |
| --- | --- |
| served by the application | 9.97e-08 |
| `chemometrics_workbench.regression.PLS` refitted on the same narrowed matrix | 7.48e-16 |
| scikit-learn `PLSRegression` on that matrix | 9.58e-16 |

On an identical matrix the two kernels agree with the arbiter and with each other. The served vector is further away because the application did not fit this exact matrix: its Savitzky-Golay and SciPy's agree to the smoothing tolerance in float64 and then round to **different float32 values at 15 of 24000 elements** - values that sit on a rounding boundary. Fitting five latent variables on a first-derivative matrix carries those half-ulp differences into `b` at the level shown, and into the predictions at the level the tables above show, which is inside their tolerance. This is the store's precision, not the arithmetic.
